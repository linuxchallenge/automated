"""Weekly market analysis reports (relative strength, index trend, breadth).

Modules are run as scripts from this directory (see weekly_analysis.py), not
imported as a package — they use flat imports like the rest of the repo.
"""
